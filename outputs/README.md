# Outputs

This directory contains finished artifacts that can be reviewed, demonstrated, or handed off:
reports, presentations, synthetic evaluation runs, and machine-readable results.

Runtime code must not import files from this directory. New generated results should be placed in
the relevant product subdirectory instead of an application directory.
