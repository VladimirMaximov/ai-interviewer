# Legacy staff API

This SQLite-based API powered the first recruiter-cabinet prototype. The staff web application now
uses the canonical FastAPI service in `apps/api/`; this implementation is retained only as a
historical reference and must not be deployed.
