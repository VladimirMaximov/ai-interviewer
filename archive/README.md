# Archive

This directory contains code that is preserved for reference but is not part of the supported
runtime. Archived applications must not be imported by code under `apps/` or included in deploy
configuration.
