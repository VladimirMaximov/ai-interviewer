# Tests

- `platform_poc/` — tests for `prototypes/interview-platform/`;
- `research_pipeline/` — tests for `tools/product-research/`;
- `output_artifacts/` — validation of checked-in synthetic outputs.

Production API tests remain next to their application in `apps/api/tests/`.
