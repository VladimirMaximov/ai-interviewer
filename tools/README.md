# Tools

Development and research utilities live here.

`product-research/` contains the resumable CLI that collects evidence, validates schemas, stores
checkpoints, and produces product reports. It is separate from the interview runtime in `apps/`.
