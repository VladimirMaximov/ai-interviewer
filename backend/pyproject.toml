[project]
name = "ai-interviewer-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "pydantic-settings>=2.5", "sqlalchemy>=2.0", "alembic>=1.13", "boto3>=1.35"]

[tool.unittest]
start-directory = "tests"
