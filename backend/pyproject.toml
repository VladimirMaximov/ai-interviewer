[project]
name = "ai-interviewer-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "pydantic-settings>=2.5"]

[tool.unittest]
start-directory = "tests"
